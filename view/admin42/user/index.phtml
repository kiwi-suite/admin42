<div class="wrapper-md bg-light b-b">
    <h1 class="m-n font-thin h3">
        <span class="fa fa-users"></span>
        <?= $this->translate('title.users', 'admin') ?>
    </h1>
</div>
<div class="wrapper-md">
    <div class="panel panel-default">
        <div class="table-responsive" ng-controller="DataGridController">
            <table st-table="collection"  st-pipe="callServer" class="table table-striped">
                <thead>
                <tr>
                    <th st-sort="id">#ID</th>
                    <th st-sort="email">E-Mail</th>
                </tr>
                <tr>
                    <th>
                        <input st-search="id" placeholder="search for id" class="input-sm form-control" type="search"/>
                    </th>
                    <th>
                        <input st-search placeholder="global search" class="input-sm form-control" type="search"/>
                    </th>
                </tr>
                </thead>
                <tbody ng-hide="isLoading">
                    <tr ng-repeat="row in collection">
                        <td>{{row.id}}</td>
                        <td>{{row.email}}</td>
                    </tr>
                </tbody>
                <tbody ng-show="isLoading">
                    <tr>
                        <td colspan="5" class="text-center"><i class="fa fa-spin fa-refresh"></i></td>
                    </tr>
                </tbody>
                <tfoot>
                <tr>
                    <td colspan="5" class="text-center">
                        <div st-pagination="" st-items-by-page="itemsByPage" st-displayed-pages="7"></div>
                    </td>
                </tr>
                </tfoot>
            </table>
        </div>
        <div class="panel-footer">
            <a class="btn btn-default" href="<?= $this->url('admin/user/add') ?>">
                <span class="fa fa-plus-circle"></span>
                <?= $this->translate('button.user-add', 'admin') ?>
            </a>
        </div>
    </div>
</div>
