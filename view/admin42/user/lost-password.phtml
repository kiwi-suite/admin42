<?php
$this->lostPasswordForm->prepare();
?>
<div class="container w-xxl w-auto-xs">
    <div class="panel">
        <div class="panel-body">
            <p class="text-center">
                <?= $this->translate('text.lostpw-description', 'admin' ) ?>
            </p>
            <form role="form" class="form-horizontal" method="post" action="<?= $this->url() ?>">
                <?= $this->formElementRender($this->lostPasswordForm->get('csrf')) ?>
                <div class="form-group<?= (count($this->lostPasswordForm->get('email')->getMessages())) ? ' has-error has-feedback' : ''?>">
                    <div class="col-sm-12">
                        <div class="input-group">
                            <span class="input-group-addon">
                                <span class="fa fa-envelope fa-fw"></span>
                            </span>
                            <?= $this->formInput($this->lostPasswordForm->get('email')->setAttributes(array(
                                'class'         => 'form-control',
                                'placeholder'   => $this->translate($this->lostPasswordForm->get('email')->getLabel(), 'admin')
                            ))) ?>
                        </div>
                        <?php if (count($this->lostPasswordForm->get('email')->getMessages())): ?>
                            <span class="glyphicon glyphicon-warning-sign form-control-feedback"></span>
                            <ul class="list-unstyled text-danger">
                                <?php foreach($this->lostPasswordForm->get('email')->getMessages() as $message): ?>
                                    <li><?= $message ?></li>
                                <?php endforeach; ?>
                            </ul>
                        <?php endif; ?>
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-sm-12">
                        <button type="submit" class="btn btn-primary btn-block">
                            <span class="fa fa-mail-forward"></span>
                            <?= $this->translate('button.recover-password', 'admin')?>
                        </button>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-12">
                        <a href="<?= $this->url('admin/login')?>" class="btn btn-default btn-block">
                            <?= $this->translate('button.cancel', 'admin') ?>
                        </a>
                    </div>
                </div>
            </form>
        </div>
        <div class="panel-footer">
            <small>
                &copy; raum42 OG
            </small>
        </div>
    </div>
</div>
