<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <?= $this->headLink() ?>

    <?= $this->headScript() ?>

    <script>
        <?= $this->admin()->angularBootstrap() ?>
    </script>
</head>

<body ng-controller="AppController">

<div class="app app-header-fixed app-aside-fixed" id="app" ng-class="{'app-aside-folded':app.settings.asideFolded}">

    <?= $this->partial('partial/admin42/topbar') ?>

    <?= $this->partial('partial/admin42/sidebar') ?>



    <div class="app-content">
        <div ui-butterbar></div>
        <a href class="off-screen-toggle hide" ui-toggle-class="off-screen" data-target=".app-aside" ></a>
        <div class="app-content-body fade-in-up">
            <?= $this->content ?>
        </div>
    </div>
</div>
<?php if (false): ?>
<?php if ($this->auth('Admin42\Authentication')->hasIdentity()): ?>
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">

    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="<?= $this->url('admin')?>"><div class="logo-sm-i"></div></a>
        </div>

        <?= $this->menu('partial/admin42/main-menu', 'admin42')->setMaxDepth(1) ?>
        <ul class="navbar-right nav navbar-nav">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <?php
                    $displayName = $this->auth('Admin42\Authentication')->getIdentity()->getDisplayName();
                    if (empty($displayName)) {
                        $displayName = $this->auth('Admin42\Authentication')->getIdentity()->getUsername();
                    }
                    if (empty($displayName)) {
                        $displayName = $this->auth('Admin42\Authentication')->getIdentity()->getEmail();
                    }
                    ?>
                    <?= $displayName ?>&nbsp;<span class="fa fa-user"></span>
                </a>
                <ul class="dropdown-menu">
                    <li>
                        <a href="<?= $this->url('admin/user/manage')?>">
                            <span class="fa fa-gear fa-fw"></span> <?= $this->translate('label.profile', 'admin')?>
                        </a>
                    </li>
                    <li>
                        <a href="<?= $this->url('admin/logout')?>">
                            <span class="fa fa-sign-out fa-fw"></span> <?= $this->translate('label.logout', 'admin')?>
                        </a>
                    </li>
                </ul>
            </li>
        </ul>
        <?php if ($this->useSidebar === true): ?>
        <button type="button" class="navbar-toggle" data-toggle="sidebar" data-target=".sidebar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <?php endif; ?>
    </div>

    <div class="divider-block horizontal"></div>
</nav>
<?php endif; ?>

<div id="main-container" class="container-fluid">
        <div class="row">
            <?php if ($this->useSidebar === true): ?>
                <div class="col-md-2 sidebar sidebar-left sidebar-animate sidebar-open">
                    <div class="box">
                        <?= $this->sidebar ?>
                    </div>
                </div>

                <div class="col-md-10 col-md-offset-2 main">
                    <?php if ($this->auth('Admin42\Authentication')->hasIdentity()): ?>
                    <?= $this->breadcrumbs('partial/admin42/breadcrumbs', 'admin42') ?>
                    <?php endif; ?>
                    <?= $this->partial('partial/admin42/alerts') ?>
                    <?= $this->content ?>
                </div>
            <?php else: ?>
                <div class="col-md-12">
                    <?php if ($this->auth('Admin42\Authentication')->hasIdentity()): ?>
                    <?= $this->breadcrumbs('partial/admin42/breadcrumbs', 'admin42') ?>
                    <?php endif; ?>
                    <?= $this->partial('partial/admin42/alerts') ?>
                    <?= $this->content ?>
                </div>
            <?php endif; ?>
        </div>
    </div>

    <div class="modal fade" id="delete-modal" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">
                        <span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
                    </button>
                    <h4 class="modal-title">Delete</h4>
                </div>
                <div class="modal-body">
                    Are you sure you want to delete this?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-danger pull-left">
                        <span class="fa fa-trash-o"></span>
                        <?= $this->translate('button.delete', 'admin') ?>
                    </button>
                    <button type="button" class="btn btn-default pull-right" data-dismiss="modal">
                        <?= $this->translate('button.cancel', 'admin') ?>
                    </button>
                    <div class="clearfix"></div>
                </div>
            </div>
        </div>
    </div>
<?php endif; ?>
</body>

</html>
