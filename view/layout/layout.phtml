<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="<?= $this->basePath("/assets/admin/css/raum42-admin.min.css") ?>" rel="stylesheet">
    <script src="<?= $this->basePath("/assets/admin/js/raum42-admin.min.js") ?>"></script>
</head>

<body>
<?php if ($this->auth('Admin42\Authentication')->hasIdentity()): ?>
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">

    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="<?= $this->url('admin')?>"><div class="logo-sm-i"></div></a>
        </div>

        <div class="collapse navbar-collapse" id="navbar-collapse-1">
            <?= $this->menu('partial/admin42/main-menu', 'admin42')->setMaxDepth(1) ?>
            <ul class="navbar-right nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <?php
                        $displayName = $this->auth('Admin42\Authentication')->getIdentity()->getDisplayName();
                        if (empty($displayName)) {
                            $displayName = $this->auth('Admin42\Authentication')->getIdentity()->getUsername();
                        }
                        if (empty($displayName)) {
                            $displayName = $this->auth('Admin42\Authentication')->getIdentity()->getEmail();
                        }
                        ?>
                        <?= $displayName ?>&nbsp;<span class="fa fa-user"></span>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a href="<?= $this->url('admin/user/manage')?>">
                                <span class="fa fa-gear" style="width: 16px;"></span> Profile
                            </a>
                        </li>
                        <li>
                            <a href="<?= $this->url('admin/logout')?>">
                                <span class="fa fa-power-off" style="width: 16px;"></span> Logout
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>

    <div class="divider-block horizontal"></div>
</nav>
<?php endif; ?>

<div id="main-container" class="container-fluid">
        <div class="row">
            <?php if ($this->useSidebar === true): ?>
                <div class="sidebar-control">
                    <button class="btn btn-link btn-xs" type="button" id="btn-sidebar-show">
                        <span class="fa fa-arrow-right"></span>
                    </button>
                </div>
                <div class="sidebar col-md-2">
                    <button class="btn btn-link btn-xs pull-right" type="button" id="btn-sidebar-hide">
                        <span class="fa fa-arrow-left"></span>
                    </button>
                    <div class="clearfix"></div>
                    <div class="box">
                        <?= $this->sidebar ?>
                    </div>
                </div>

                <div class="main col-md-10 col-md-offset-2">
                    <?php if ($this->auth('Admin42\Authentication')->hasIdentity()): ?>
                    <?= $this->breadcrumbs('partial/admin42/breadcrumbs', 'admin42') ?>
                    <?php endif; ?>
                    <?= $this->partial('partial/admin42/alerts') ?>
                    <?= $this->content ?>
                </div>
            <?php else: ?>
                <div class="col-md-12">
                    <?php if ($this->auth('Admin42\Authentication')->hasIdentity()): ?>
                    <?= $this->breadcrumbs('partial/admin42/breadcrumbs', 'admin42') ?>
                    <?php endif; ?>
                    <?= $this->partial('partial/admin42/alerts') ?>
                    <?= $this->content ?>
                </div>
            <?php endif; ?>
        </div>
    </div>

    <div class="modal fade" id="delete-modal" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">
                        <span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
                    </button>
                    <h4 class="modal-title">Delete</h4>
                </div>
                <div class="modal-body">
                    Are you sure you want to delete this?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-danger pull-left">
                        <span class="fa fa-trash-o"></span>
                        Delete
                    </button>
                    <button type="button" class="btn btn-default pull-right" data-dismiss="modal">Close</button>
                    <div class="clearfix"></div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
