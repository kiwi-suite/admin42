<?php
$submitContent = <<<EOT
<button class="btn btn-addon {{ btnClass }}" ng-class="submitLoading ? 'disabled' : ''" type="submit">
    <i><span ng-class="submitLoading ? 'fa fa-spin fa-refresh' : icon"></span></i>
    {{ submitText }}
</button>
EOT;
$this->angular()->addHtmlTemplate("element/submit.html", $submitContent);
